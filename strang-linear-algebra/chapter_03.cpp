/* 
Gilbert Strang's Linear Algebra
Covers the second part of chapter 2 (linear transformations)
and continues into chapter 3 (orthogonality, least squares, Fourier)

Objectives:
- Get more hands-on experience with the following packages:
  - `Eigen`:  — matrix storage and all linear algebra (the transformation matrices themselves, matrix-vector multiply, etc.)
  - `OpenGL` via `Pangolin` wrapper: window creation, the render loop, UI controls (buttons/sliders), and issuing the raw OpenGL draw calls
*/

#include <Eigen/Dense>
#include <pangolin/pangolin.h>

int main()
{
  return 0;
}